import { createTheme } from "@mui/material";

const theme = createTheme({
  pallete: {
    primary: {
      main: "#fff",
      light: "#fff",
      dark: "#0000",
    },
    background: { paper: "#304FFE", default: "#304FFE" },
  },
});

export default theme;
